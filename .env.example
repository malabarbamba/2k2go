# Frontend boot can read either runtime-config.json or Vite env vars.
# Copy these into a local .env only if you do not want to use public/runtime-config.json.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
